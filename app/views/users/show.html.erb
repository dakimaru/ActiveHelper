<% provide(:title, @user.name) %>
<div class="row">
	<aside class="span2">
			<%= render 'avatar' %>
			<%= render 'follow_form' if signed_in_user? %>
			<%= render 'shared/stats' %>
		<section>
			MEMBER OF  <%= @user.organizations.count%>
		</section>
	</aside>
	
	<div class="span10">
		<%= render 'profile_info' %>
		
		<div class="users">
		<h2>MEDIA</h2>
		
		<ul><%= render "avatar"%></ul>
		
		<h2>ACTIVITY</h2>
		<div class="well">
			
		</div>
		
		</div>
	</div>
</div>
