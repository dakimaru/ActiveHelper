<%= form_for(current_user.memberships.build(organization_id: @organization.id),
             remote: true) do |f| %>
  <div><%= f.hidden_field :organization_id %></div>
  <%= f.submit "Join", class: "btn btn-large btn-primary" %>
<% end %>