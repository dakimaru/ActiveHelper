<%= form_for(current_user.memberships.find_by_organization_id(@organization),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Leave", class: "btn btn-large" %>
<% end %>